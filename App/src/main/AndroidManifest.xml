<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.retask"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- android:debuggable=false required when making a release build -->

    <application
        android:debuggable="false"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="Retask"
        android:name="me.retask.RetaskApplication"
        android:theme="@style/AppTheme">

        <activity android:name="me.retask.activities.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="me.retask.activities.SignedUpActivity" />
        <activity android:name="me.retask.activities.HomeActivity" />
        <activity android:name="me.retask.activities.ViewTaskActivity" />
        <activity android:name="me.retask.activities.CreateTaskActivity" />
        <activity android:name="me.retask.activities.EditTaskActivity" />
    </application>

</manifest>
