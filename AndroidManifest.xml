<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.loki2302"
    android:versionName="1.0-SNAPSHOT" >

    <uses-sdk android:minSdkVersion="4" android:targetSdkVersion="4" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="Retask"
        android:theme="@style/AppTheme">
        
        <activity android:name="me.loki2302.activities.alt.SignInActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- <activity android:name="me.loki2302.activities.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->
        
        <activity android:name="me.loki2302.activities.WorkspaceActivity" />
        <activity android:name="me.loki2302.activities.TaskActivity" />
        <activity android:name="me.loki2302.activities.CreateTaskActivity" />
    </application>
</manifest>